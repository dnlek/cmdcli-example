{
  "name": "cmdcli-test",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "bin": {
    "test": "node_modules/cmdcli/lib/index"
  },
  "scripts": {
    "compile": "babel -d ./commands src/",
    "prepublish": "npm run compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-1": "^6.5.0"
  },
  "dependencies": {
  }
}
